# Project Rules

## Security Constraints
- **Never commit or push `.env` or `.env.production` files.** These must always remain local and be explicitly added to `.gitignore`.
- Ensure all environment credentials or configuration files are kept secure and never tracked by Git.
